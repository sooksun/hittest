Options -Indexes
RewriteEngine On
RewriteBase /newhittest/api/

# Serve real PHP files directly (e.g. tts.php)
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]

# Everything else (extension-less + dynamic segments) → front controller
RewriteRule ^ index.php [QSA,L]
